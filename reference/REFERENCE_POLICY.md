# 上游材料管理规则

1. 当前改进版是唯一维护和运行对象。
2. `reference/upstream/` 只读使用，不参与 Python 包发现、测试收集或 CLI。
3. 上游快照必须绑定仓库 URL、branch、完整 commit SHA、许可证和 SHA256。
4. 不在上游快照内直接修补文件；我们的修改必须进入根目录当前代码。
5. 上游更新采用“新增快照 + 新差异报告”，不覆盖旧快照。
6. 发布当前项目时保留 GPL v3、上游项目链接和 Antoine Tavant 原始 PIC 归属。
